#include <stdio.h>
//"include" information about the c standard IO library
int main(void) //programs necessary code goes inside the main
{
    printf("To C, or not to C: that is the question.\n"); // \n create sthe new line
    return 0; // returns the value of 0 to the OS when terminated
}

